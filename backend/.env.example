# Example env for backend
# Use Firestore (recommended): provide either a path to a service account JSON file
# or the JSON contents directly (useful for container env vars).

# Path to service account JSON (absolute or relative to repo)
FIREBASE_CREDENTIALS_PATH=./path/to/serviceAccountKey.json

# Or the JSON contents (single-line JSON string). Use one of the two methods above.
FIREBASE_CREDENTIALS_JSON=

# Optional: legacy SQLite fallback path
SQLITE_PATH=mediscribe.db

# Optional: Postgres DATABASE_URL (if you'd prefer Postgres over Firestore)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Gemini API (server-side)
# Set these to enable server-side Gemini enrichment. Do NOT put keys in frontend.
GEMINI_API_KEY=
GEMINI_API_URL=
# Example backend environment variables
# To use Postgres, set DATABASE_URL to a valid Postgres connection string, e.g.
# postgres://user:password@localhost:5432/mediscribe
DATABASE_URL=

# If you prefer SQLite, leave DATABASE_URL empty and optionally set SQLITE_PATH
SQLITE_PATH=mediscribe.db

# GEMINI_API_KEY (if you integrate Gemini on the server)
GEMINI_API_KEY=